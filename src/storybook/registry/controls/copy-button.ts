import { DemoControl } from "../types";

export const controls: DemoControl[] = [
      {
        type: "text",
        param: "text",
        label: "Text to copy",
        defaultValue: "npm install one-for-all",
      },
    ];