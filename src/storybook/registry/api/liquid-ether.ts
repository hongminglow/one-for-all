import { ApiProp } from "../types";

export const api: ApiProp[] = [
      {
        prop: "resolution",
        type: "number",
        default: "1",
        description: "Resolution scale",
      },
    ];